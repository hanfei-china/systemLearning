/*
    1. position
        1. static  默认值，没有定位
        2. relative  相对定位，相对其正常位置进行定位。如果一个元素使用了relative，然后设置了left为20px，那么就会对这个元素的正常位置的left添加20像素
        3. absolute  绝对定位。相对于static定位以外的第一个父元素进行定位，如果没有，那么就会相对于根元素进行定位。
        4. fixed   固定定位，相对于浏览器可视窗口进行定位。
    2. float
*/